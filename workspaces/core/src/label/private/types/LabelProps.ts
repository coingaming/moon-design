type LabelProps = {
  color?: string;
  bgColor?: string;
  iconLeft?: React.ReactElement;
  iconRight?: React.ReactElement;
  size?: '2xs' | 'xs';
  isUppercase?: boolean;
};

export default LabelProps;
