import { Tabs } from '@heathmont/moon-core-tw';
import React from 'react';

const Example = () => {
  const item = ['$Link', '$Link'];
  
  return <Tabs items={item} isTabPills />
};

export default Example;
