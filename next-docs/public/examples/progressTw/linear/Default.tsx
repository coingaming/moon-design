import React from 'react';
import { ProgressLinear } from '@heathmont/moon-core-tw';

const Example = () => (
  <>
    <ProgressLinear
      value={30}
      // text={<span>Progress</span>}
      backgroundColor="bg-goten"
      size='6xs'
    />
    <ProgressLinear
      value={30}
      // text={<span>Progress</span>}
      backgroundColor="bg-goten"
      size='5xs'
    />
      <ProgressLinear
      value={30}
      // text={<span>Progress</span>}
      backgroundColor="bg-goten"
      size='4xs'
    />
    <ProgressLinear
      value={30}
      // text={<span>Progress</span>}
      backgroundColor="bg-goten"
      size='3xs'
    />
     <ProgressLinear
      value={30}
      // text={<span>Progress</span>}
      backgroundColor="bg-goten"
      size='2xs'
    />
  </>
);

export default Example;
