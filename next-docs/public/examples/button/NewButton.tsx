import { Button } from '@heathmont/moon-core';
import { IconProfile } from '@heathmont/moon-assets';

const NewButton = () => (
  <Button iconRight={<IconProfile color="#fff" />}>New Button</Button>
);

export default NewButton;
