type Base = {
  fontSize: number;
  lineHeight: number;
  fontFace: string;
  spacing: number;
};

const base: Base = {
  fontSize: 16,
  lineHeight: 20,
  fontFace: 'AvertaStd',
  spacing: 16,
};

export { Base, base };
