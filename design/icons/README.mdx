---
name: Icons
route: /icons
---

import { PropsTable } from 'docs';

# Icons

Icons offer visual aids for common actions and tasks.

To aid with performance, Icons are surfaced as individual [SVG](https://developer.mozilla.org/en-US/docs/Web/SVG) components. Each icon component is prefixed with `Icon`.

## Categories

### Chevrons and Arrows

```jsx react-live
<IconChevronDown />
<IconChevronLeft />
<IconChevronRight />
<IconChevronUp />
<IconChevronUpDown />
```

```jsx react-live
<IconArrowUp />
<IconArrowRight />
<IconArrowDown />
<IconArrowLeft />
<IconArrowUpDown />
<IconArrowLeftRight />
<IconLeftArrowCurveRight />
```

### Currency

```jsx react-live
<IconEuro />
<IconBitcoin />
```

### Actions

```jsx react-live
<IconClose />
<IconSettings />
<IconFilter />
<IconFreebet />
<IconEdit/>
<IconHome />
<IconProfile />
<IconSearch />
<IconCashOut />
<IconStats />
<IconPriceBoost />
<IconPlay />
```

### Forms

```jsx react-live
<IconSuccess />
<IconWarning />
<IconUpload />
```

### Shapes

```jsx react-live
<IconCircle />
```

## Customizing

### Color

Icon colors can be modified by adding the `color` attribute/CSS property to the component or its parent element.

```jsx react-live
<IconSettings color={colors.brand} />
```

There's also the option to define a `backgroundColor`.

Note: for inline SVGs you may need to apply an additional `background-image` of
a circle behind the icon.

```jsx react-live
<IconClose color={colors.neutral[10]} backgroundColor={colors.error} />
```

### Size

Icon size can be modified by adding the `fontSize` attribute or `font-size` CSS property to the component or its parent element.

```jsx react-live
<IconSettings fontSize="40px" />
```

## Contributing

For information on creating new/modifying existing icons, see [the contributing guidelines](https://github.com/coingaming/sportsbet-design/blob/develop/CONTRIBUTING.md#maintaining-assets).
