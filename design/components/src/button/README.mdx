---
name: Button
menu: Components
route: /components/button
---

import { Button } from '@heathmont/sportsbet-components';
import { PropsTable } from 'docs';

# Button

## Modifiers

### Primary

To be used where the the button would be the main call to action on the page.

```jsx react-live
<Button modifier="primary">
  Default
</Button>
<Button modifier="primary" mockState="hover" >
  Hover
</Button>
<Button modifier="primary" mockState="active" >
  Focus
</Button>
<Button modifier="primary" disabled >
  Disabled
</Button>
```

### Highlight

To be used where the the button would be a highlight call to action.

```jsx react-live
<Button modifier="highlight" >
  Default
</Button>
<Button modifier="highlight" mockState="hover" >
  Hover
</Button>
<Button modifier="highlight" mockState="active" >
  Focus
</Button>
<Button modifier="highlight" disabled >
  Disabled
</Button>
```

### Secondary

To be used where the the button would be a secondary call to action.

```jsx react-live
<Button modifier="secondary" >
  Default
</Button>
<Button modifier="secondary" mockState="hover" >
  Hover
</Button>
<Button modifier="secondary" mockState="active" >
  Focus
</Button>
<Button modifier="secondary" disabled >
  Disabled
</Button>
```

### Optional

To be used where the the button would be a optional call to action.

```jsx react-live
<Button modifier="optional" >
  Default
</Button>
<Button modifier="optional" mockState="hover" >
  Hover
</Button>
<Button modifier="optional" mockState="active" >
  Focus
</Button>
<Button modifier="optional" disabled >
  Disabled
</Button>
```

## Outline

To be used where the button would be a alternate call to action.

```jsx react-live
<div>
  <Button modifier="primary" outline>
    Default
  </Button>
  <Button modifier="primary" mockState="hover" outline >
    Hover
  </Button>
  <Button modifier="primary" mockState="active" outline >
    Focus
  </Button>
  <Button modifier="primary" disabled outline >
    Disabled
  </Button>
</div>
<div>
  <Button modifier="highlight" outline>
    Default
  </Button>
  <Button modifier="highlight" mockState="hover" outline >
    Hover
  </Button>
  <Button modifier="highlight" mockState="active" outline >
    Focus
  </Button>
  <Button modifier="highlight" disabled outline>
    Disabled
  </Button>
</div>
<div>
  <Button modifier="secondary" outline>
    Default
  </Button>
  <Button modifier="secondary" mockState="hover" outline>
    Hover
  </Button>
  <Button modifier="secondary" mockState="active" outline>
    Focus
  </Button>
  <Button modifier="secondary" disabled outline>
    Disabled
  </Button>
</div>
```

## Round

```jsx react-live
<Button round modifier="primary" >
  Round
</Button>
<Button round modifier="primary" mockState="hover" >
  Hover
</Button>
<Button round modifier="primary" mockState="active" >
  Focus
</Button>
<Button round modifier="primary" disabled >
  Disabled
</Button>
```

## Uppercase

To be used where the the button caption would be uppercased.

```jsx react-live
<Button modifier="primary" uppercase>
  Uppercased primary
</Button>
<Button modifier="secondary" uppercase >
  Uppercased secondary
</Button>
```

## Sizing

### Full Width

Stretches a button to fill the entire width of its container.

```jsx react-live
<Button modifier="secondary" fullWidth>
  Full Width
</Button>
```

## Elements

### Anchor

Sometimes we want to use the Button component to send the user to a different location.

By adding the `as="a"` attribute, Buttons can be rendered as `<a>` elements.

Don't forget to pass `href="<url>"`

```jsx react-live
<Button href="#" as="a" modifier="secondary">
  Secondary (Anchor)
</Button>
```

## Props

<PropsTable of={Button} />
