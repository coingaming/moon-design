import { cdnUrl } from '../';

const VERSION = 'v1';

export const fontUrls = {
  AvertaStd: {
    bold: `${cdnUrl}/fonts/AvertaStd/bold-${VERSION}.woff2`,
    regular: `${cdnUrl}/fonts/AvertaStd/regular-${VERSION}.woff2`,
    semibold: `${cdnUrl}/fonts/AvertaStd/semibold-${VERSION}.woff2`,
  },
};
