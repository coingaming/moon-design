import styled from 'styled-components';

const Container = styled.div({
  position: 'relative',
  zIndex: 0,
  cursor: 'pointer',
  display: 'flex',
  alignItems: 'center',
});

export default Container;
