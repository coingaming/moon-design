type Breakpoints = {
  small: number /* in `px` */;
  medium: number /* in `px` */;
  large: number /* in `px` */;
  xlarge: number /* in `px` */;
};

const breakpoints: Breakpoints = {
  small: 500,
  medium: 800,
  large: 1080,
  xlarge: 1200,
};

export { Breakpoints, breakpoints };
