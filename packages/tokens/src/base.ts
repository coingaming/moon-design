type Base = {
  fontSize: number;
  lineHeight: number;
  fontFace: string;
  spacing: number;
  container: number;
};

const base: Base = {
  fontSize: 16,
  lineHeight: 20,
  fontFace: "Averta",
  spacing: 16,
  container: 1400
};

export { Base, base };
