---
name: Progress Circular
menu: Components
route: /components/progress-circular
---

# Progress Circular

[JIRA link](https://coingaming.atlassian.net/wiki/spaces/CDS/pages/2033778811/Progress+indicator)

A progress indicator is a visual representation of a user’s progress through a set of steps, guiding toward the completion of a specified process.

## Usage

Use this when the process has a known duration

Circular progress indicators display progress by animating an indicator along an invisible circular track in a clockwise direction.

```jsx react-live
<ProgressCircular value={30} fontSize="5rem" />
```

### strokeWidth

Default value:

```
strokeWidth = 12
```

```jsx react-live
<Inline>
  <ProgressCircular value={30} fontSize="5rem" strokeWidth={12} />
  <ProgressCircular value={30} fontSize="5rem" strokeWidth={2} />
</Inline>
```

## See also

If you need a [linear progress indicator](/components/progress-linear)
