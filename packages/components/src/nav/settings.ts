import { spacing } from '@heathmont/sportsbet-utils';
import { border } from '@heathmont/sportsbet-tokens';

const underlineWidth = border.width * 2;
const underlineOffset = spacing('small');

export { underlineOffset, underlineWidth };
