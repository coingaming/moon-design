---
name: Button
menu: Components
route: /components/button
---

import { Button } from '@heathmont/moon-components';
import { Loader } from '@heathmont/moon-components';
import { PropsTable } from 'docs';

# Button

[Figma link](https://www.figma.com/file/S3q1SkVngbwHuwpxHKCsgtJj/Global?node-id=22%3A751)

## Variants

### Primary

To be used where the the button would be the main call to action on the page.

```jsx react-live
<Inline>
  <Button variant="primary">Default</Button>
  <Button variant="primary" mockState="hover">
    Hover
  </Button>
  <Button variant="primary" mockState="active">
    Focus
  </Button>
  <Button variant="primary" disabled>
    DisabledTW
  </Button>
</Inline>
```

### Secondary

To be used where the the button would be a secondary call to action.

```jsx react-live
<Inline>
  <Button variant="secondary">Default</Button>
  <Button variant="secondary" mockState="hover">
    Hover
  </Button>
  <Button variant="secondary" mockState="active">
    Focus
  </Button>
  <Button variant="secondary" disabled>
    DisabledTW
  </Button>
</Inline>
```

### Tertiary

To be used where the the button would be a optional call to action.

```jsx react-live
<Inline>
  <Button variant="tertiary">Default</Button>
  <Button variant="tertiary" mockState="hover">
    Hover
  </Button>
  <Button variant="tertiary" mockState="active">
    Focus
  </Button>
  <Button variant="tertiary" disabled>
    DisabledTW
  </Button>
</Inline>
```

### Highlight

To be used where the the button would be a highlight call to action.

```jsx react-live
<Inline>
  <Button variant="highlight">Default</Button>
  <Button variant="highlight" mockState="hover">
    Hover
  </Button>
  <Button variant="highlight" mockState="active">
    Focus
  </Button>
  <Button variant="highlight" disabled>
    DisabledTW
  </Button>
</Inline>
```

### Default

To be used where the the button would be a default call to action.

```jsx react-live
<Inline>
  <Button>Default</Button>
  <Button mockState="hover">Hover</Button>
  <Button mockState="active">Focus</Button>
  <Button disabled>DisabledTW</Button>
</Inline>
```

### Animations

#### User feedback on the action

```jsx react-live
<Inline>
  <Button variant="primary">Default</Button>
  <Button progress variant="primary">
    Default
  </Button>
  <Button success variant="primary">
    Default
  </Button>
  <Button oops variant="primary">
    Default
  </Button>
</Inline>
```

#### Call to action animations

```jsx react-live
<Button pulse variant="primary">
  Default
</Button>
```

### Icons

```jsx react-live
<>
  <Inline>
    <Button
      iconLeft={<IconProfile color="#fff" />}
      variant="primary"
      size="xsmall"
    >
      Default
    </Button>
    <Button
      iconRight={<IconProfile color="#fff" />}
      variant="primary"
      size="xsmall"
    >
      Default
    </Button>
  </Inline>

  <Inline>
    <Button iconLeft={<IconProfile color="#fff" />} variant="primary">
      Default
    </Button>
    <Button iconRight={<IconProfile color="#fff" />} variant="primary">
      Default
    </Button>
  </Inline>

  <Inline>
    <Button
      iconLeft={<IconProfile color="#fff" />}
      variant="primary"
      size="medium"
    >
      Default
    </Button>
    <Button
      iconRight={<IconProfile color="#fff" />}
      variant="primary"
      size="medium"
    >
      Default
    </Button>
  </Inline>
  <Inline>
    <Button
      iconLeft={<IconProfile color="#fff" />}
      variant="primary"
      size="large"
    >
      Default
    </Button>
    <Button
      iconRight={<IconProfile color="#fff" />}
      variant="primary"
      size="large"
    >
      Default
    </Button>
  </Inline>
</>
```

```jsx react-live
<Stack>
  <Button
    fullWidth
    iconLeft={<IconProfile color="#fff" />}
    variant="primary"
    size="xsmall"
  >
    Default
  </Button>
  <Button
    fullWidth
    iconRight={<IconProfile color="#fff" />}
    variant="primary"
    size="xsmall"
  >
    Default
  </Button>

  <Button fullWidth iconLeft={<IconProfile color="#fff" />} variant="primary">
    Default
  </Button>
  <Button fullWidth iconRight={<IconProfile color="#fff" />} variant="primary">
    Default
  </Button>

  <Button
    fullWidth
    iconLeft={<IconProfile color="#fff" />}
    variant="primary"
    size="medium"
  >
    Default
  </Button>
  <Button
    fullWidth
    iconRight={<IconProfile color="#fff" />}
    variant="primary"
    size="medium"
  >
    Default
  </Button>

  <Button
    fullWidth
    iconLeft={<IconProfile color="#fff" />}
    variant="primary"
    size="large"
  >
    Default
  </Button>
  <Button
    fullWidth
    iconRight={<IconProfile color="#fff" />}
    variant="primary"
    size="large"
  >
    Default
  </Button>
</Stack>
```

### Sizes

```jsx react-live
<Inline>
  <Button variant="primary" size="xsmall">
    Xsmall
  </Button>
  <Button variant="primary" size="small">
    Small
  </Button>
  <Button variant="primary" size="medium">
    Medium
  </Button>
  <Button variant="primary" size="large">
    Large
  </Button>
</Inline>
```

## Sizing

### Full Width

Stretches a button to fill the entire width of its container.

```jsx react-live
<Button variant="secondary" fullWidth>
  Full Width
</Button>
```

## Elements

### Anchor

Sometimes we want to use the Button component to send the user to a different location.

By adding the `as="a"` attribute, Buttons can be rendered as `<a>` elements.

Don't forget to pass `href="<url>"`

```jsx react-live
<Button href="#" as="a" variant="secondary">
  Secondary (Anchor)
</Button>
```
