---
name: Carousel
menu: Components
route: /components/carousel
---

# Carousel

An interactive extension of the [`Reel`](../reel) component to cycle through content.

```jsx react-live
() => {
  const items = Array.from({ length: 25 }, (value, index) => index);

  const ExampleContent = styled.div(({ theme: { color, radius } }) => ({
    width: '20rem',
    height: '12rem',
    borderRadius: radius.small,
    background: color.goku[80],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  }));

  const Container = styled.div({
    maxWidth: '600px',
  });

  return (
    <Container>
      <Carousel
        scrollTo={5}
        items={items.map((item, index) => (
          <ExampleContent>{index}</ExampleContent>
        ))}
      />
    </Container>
  );
};
```

## Visible indices

```jsx react-live
() => {
  const items = Array.from({ length: 5 }, (value, index) => index);
  const ExampleContent = styled.div(({ theme: { color, radius } }) => ({
    width: '20rem',
    height: '12rem',
    borderRadius: radius.small,
    background: color.goku[80],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '2rem',
  }));
  return (
    <Carousel
      space="xlarge"
      items={({ firstVisibleIndex, lastVisibleIndex }) =>
        items.map((item, index) => (
          <ExampleContent>
            {firstVisibleIndex}, {lastVisibleIndex}
          </ExampleContent>
        ))
      }
    />
  );
};
```

## Customization

### Carousel with customized arrows

Use these props

- `scrollToLeftButton`
- `scrollToRightButton`

They provide a function with `scrollToStep` and `disabled` arguments that you could use in your custom elements.

```jsx react-live
() => {
  const items = Array.from({ length: 25 }, (value, index) => index);

  const ExampleContent = styled.div(({ theme: { color, radius } }) => ({
    width: '20rem',
    height: '12rem',
    borderRadius: radius.small,
    background: color.goku[80],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '2rem',
  }));

  return (
    <Carousel
      scrollToLeftButton={({ scrollToStep, disabled }) => (
        <button disabled={disabled} onClick={scrollToStep}>
          Scroll to left
        </button>
      )}
      scrollToRightButton={({ scrollToStep, disabled }) => (
        <button disabled={disabled} onClick={scrollToStep}>
          Scroll to Right
        </button>
      )}
      items={items.map((item, index) => (
        <ExampleContent>{index}</ExampleContent>
      ))}
    />
  );
};
```

### Space

```jsx react-live
() => {
  const items = Array.from({ length: 5 }, (value, index) => index);

  const ExampleContent = styled.div(({ theme: { color, radius } }) => ({
    width: '20rem',
    height: '12rem',
    borderRadius: radius.small,
    background: color.goku[80],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '2rem',
  }));

  return (
    <Carousel
      space="xlarge"
      items={items.map((item, index) => (
        <ExampleContent>{index}</ExampleContent>
      ))}
    />
  );
};
```

### Horizontal Edge Gap

Add space before the first item and after the last item.

```jsx react-live
() => {
  const items = Array.from({ length: 5 }, (value, index) => index);

  const ExampleContent = styled.div(({ theme: { color, radius } }) => ({
    width: '20rem',
    height: '12rem',
    borderRadius: radius.small,
    background: color.goku[80],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '2rem',
  }));

  return (
    <Carousel
      horizontalEdgeGap={20}
      items={items.map((item, index) => (
        <ExampleContent>{index}</ExampleContent>
      ))}
    />
  );
};
```

### Hide Scrollbar

```jsx react-live
() => {
  const items = Array.from({ length: 5 }, (value, index) => index);

  const ExampleContent = styled.div(({ theme: { color, radius } }) => ({
    width: '20rem',
    height: '12rem',
    borderRadius: radius.small,
    background: color.goku[80],
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '2rem',
  }));

  return (
    <Carousel
      hideScrollbar
      items={items.map((item, index) => (
        <ExampleContent>{index}</ExampleContent>
      ))}
    />
  );
};
```

Spacing between adjacent content can be adjusted via the `space` prop. Same as `Reel` component.

`space` accepts themed space keys (e.g. `large` returns the large themed `space`
key in `rem`), a number (returned in `px`) or a string value (e.g. `"30px"` or
`"1rem"`).

## **withHorizontalScroll hook**

By invoking withHorizontalScroll, you will receive the following methods:

- `containerRef` - set ref for scroll area (not necessary param, set browser viewport by default);

- `itemRef` - set ref for each item inside scroll area (necessary param);

- `scrollLeftToStep, scrollRightToStep` - scrolling with step setted as scrollStep;

- `scrollToIndex` - func, scrolling to needed index: scrollToIndex(index);

- `canScrollLeft, canScrollRight` - bool, settings show or hide arrows;

withHorizontalScroll also has optional argument:

- `scrollStep` - number, step for left and right scroll

```

```
